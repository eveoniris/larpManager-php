database:
    driver: pdo_mysql #TODO resoudre le probleme de creation d'index pour passer en sqlite
    host: 127.0.0.1
    dbname: db_test
    user: root
    password: 
    #memory: true #pour sqlite in memory (rapide !)

db_install_path : /../tests/Temporary/