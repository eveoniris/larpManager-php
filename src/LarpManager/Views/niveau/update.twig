{% extends "layout.twig" %}

{% block content %}

	<a href="{{ path("niveau") }}">Retour à la liste des niveaux</a>
	
	<div class="page-header">
		<h1>Modification de {{ niveau.label }}</h1>
	</div>

	<div class="container-fluid">
		<div class="row">
			<div class="col-xs-12 col-md-8">
				{{ include("niveau/fragment/form.twig",{'action': path('niveau.update',{'index': niveau.id}), 'form' : form}) }}
			</div>
		</div>
	</div>
		
{% endblock content %}