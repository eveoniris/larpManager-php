{% extends "layout.twig" %}

{% block title %}{{ regle.label }}{% endblock title %}

{% block content %}

<ol class="breadcrumb">
	<li><a href="{{ path('homepage')  }}">Accueil</a></li>
	<li><a href="{{ path('rules') }}">Liste des règles</a></li>
	<li class="active">{{ rule.label }}</li>
</ol>


<div class="well well-sm">
	<div class="header">
		<h5>{{ rule.label }}</h5>
	</div>
	
	<blockquote>{{ rule.description|markdown }}</blockquote>
	
	<object type="application/pdf" width="100%" height="1200px" >
		<param name="src" value="{{ path('rule.document',{'rule' :rule.id}) }}" />
	 
		<p>It appears your Web browser is not configured to display PDF files. No worries, just <a href="{{ path('participant.regle.document',{'participant': participant.id, 'rule' : regle.id}) }}">click here to download the PDF file.</a></p>
	</object>

</div>
{% endblock content %}