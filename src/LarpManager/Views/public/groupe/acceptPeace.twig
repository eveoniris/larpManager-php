{% extends "layout.twig" %}

{% block title %}Accepter la paix{% endblock title %}

{% block content %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Page d'accueil</a></li>
		<li><a href="{{ path('groupe') }}">Votre groupe</a></li>
		<li class="active">Accepter la paix</li>
	</ol>
	
	<div class="well bs-component">
	
		<form action="{{ path('groupe.acceptPeace', {'groupe': groupe.id, 'enemy': war.id}) }}" method="POST" {{ form_enctype(form) }} novalidate>
			{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}

			<p>Vous allez accepter la proposition de paix dans le conflit qui oppose <strong>{{ war.groupe.nom }}</strong> et <strong>{{ war.requestedGroupe.nom }}</strong>.

			{{ form(form) }}
			
			<p>Le chef de groupe recevra une notification pour le prévenir de votre choix.</p>
			 
		</form>
	
	</div>
	
{% endblock %}