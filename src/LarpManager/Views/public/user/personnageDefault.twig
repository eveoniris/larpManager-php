{% extends "layout.twig" %}

{% block title %}Restrictions alimentaires{% endblock title %}

{% block content %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li class="active">Mes personnage par défaut</li>
	</ol>
	
	<div class="well well-sm">
		<form action="{{ path('user.personnageDefault', {'user': user.id}) }}" method="POST" {{ form_enctype(form) }}>
			<fieldset>
				{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}
				{{ form(form) }}
			</fieldset>
		</form>
	</div>
	
{% endblock content%}