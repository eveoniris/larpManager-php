{% extends "layout.twig" %}

{% block title %}{{ personnage.publicName }}{% endblock title %}

{% block content %}
<div class="container-fluid">

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage')  }}">Accueil</a></li>
		<li><a href="{{ path('personnage')  }}">Votre personnage</a></li>
		<li class="active">Choix d'une origine</li>
	</ol>
	
	<div class="well bs-component">
		<form action="{{ path('personnage.origin.add',{'personnage':personnage.id}) }}" method="POST" {{ form_enctype(form) }}>
			<fieldset>
				<legend>{{ personnage.publicName }} - <small>Choix d'une origine</small></legend>
				<p>Choisissez l'origine de votre personnage, en général, les personnages sont issus du même pays que celui de leur groupe !</p>
				<p>Attention ! une fois votre origine choisie, vous ne pourrez plus revenir sur votre choix.</p>
				{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}
	
				{{ form_row(form.territoire) }}
				{{ form_rest(form) }}
			</fieldset>
		</form>
	</div>
{% endblock content %}