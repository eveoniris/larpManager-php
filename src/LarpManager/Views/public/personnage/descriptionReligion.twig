{% extends "layout.twig" %}

{% block title %}{{ personnage.publicName }}{% endblock title %}

{% block content %}

<div class="container-fluid">

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage')  }}">Accueil</a></li>
		<li><a href="{{ path('participant.index', {'participant' : participant.id}) }}">{{ participant.gn.label }}</a></li>
		<li><a href="{{ path('participant.personnage', {'participant': participant.id})  }}">{{ personnage.nom }} {{ personnage.surnom }}</a></li>
		<li class="active">Choix d'un nouveau descriptif de religion</li>
	</ol>
	
	<div class="well bs-component">
	<form action="{{ path('participant.personnage.religionDescription',{'participant':participant.id}) }}" method="POST" {{ form_enctype(form) }}>
		<fieldset>
			<blockquote>Votre compétence vous permet de choisir une nouvelle description de religion.</blockquote>

			{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}
			
			{{ form_row(form.religion) }}
			{{ form_rest(form) }}
		</fieldset>
	</form>
	</div>
</div>

{% endblock content %}