{% extends "layout.twig" %}

{% block title %}Trombine{% endblock title %}

{% block content %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Page d'accueil</a></li>
		<li><a href="{{ path('gn.detail', {'gn' : participant.gn.id}) }}">{{ participant.gn.label }}</a></li>
		<li><a href="{{ path('gn.personnage', {'gn': participant.gn.id}) }}">{{ participant.personnage.nom }}</a></li>
		<li class="active">Mise à jour de votre photo</li>
	</ol>
	
	<div class="well bs-component">
	
		<blockquote>Téléversez une photo de vous pour le trombinoscope. Ce trombinoscope sera visible par les orgas, et les membres de vos groupes.</blockquote>
								
		<div class="header"><h5>Votre trombine</h5></div>

		{% if personnage.trombineUrl %}		
			<img src="{{ path('personnage.trombine', {'personnage' : personnage.id }) }}" />
		{% else %}
			<img src="{{ app.request.basepath }}/img/no_trombine.png" />
		{% endif %}
		
		<div class="header"><h5>Modifier votre photo</h5></div>
		
		<form action="{{ path('participant.personnage.trombine', {'participant': participant.id, 'personnage': personnage.id}) }}" method="POST" {{ form_enctype(form) }}>
				{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}
				{{ form(form) }}
		</form>
			
	</div>
	
{% endblock content %}