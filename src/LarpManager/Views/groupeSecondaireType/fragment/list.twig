<ul class="list-group">
	{%  if add != false %}
		<a href="{{ path('groupeSecondaire.type.add') }}" class="list-group-item active">
			<span class="badge"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span></span>
			<h4 class="list-group-item-heading">Ajouter un type de groupe secondaire</h4>
		</a>
	{%  endif %}
	{%  for groupeSecondaireType in groupeSecondaireTypes %}
	<li class="list-group-item">
		<h4 class="list-group-item-heading">
			{{ groupeSecondaireType.label }}
			<div class="btn-group pull-right" role="group" aria-label="...">
				<a href="{{ path('groupeSecondaire.type.detail', {index: groupeSecondaireType.id}) }}" class="btn btn-primary" role="button">Voir</a>
			    <a href="{{ path('groupeSecondaire.type.update', {index: groupeSecondaireType.id}) }}" class="btn btn-default" role="button">Modifier</a>
			</div>
		</h4>
	</li>
	{%  endfor %}
</ul>