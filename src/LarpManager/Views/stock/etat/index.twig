{% extends "layout.twig" %}

{% block content %}

	<div id="ajax_entity">
		<table class="pure-table pure-table-horizontal pure-table-striped">
		<caption>Liste des états</caption>
		<thead>
			<tr>
				<th>Label</th>
				<th>Nombre d'objets</th>
				<th colspan="2"><a class="ajax_action" href="{{  path('stock_etat_add') }}"><i class="icon-plus-sign"></i></a></th>
			</tr>
		</thead>
		<tbody>
		{% for etat in etats %}
		<tr>
		<td>{{ etat.label }}</td>
		<td>{{ etat.objets|length }}</td>
		<td><a href=""><i class="icon-document"></i></a></td>
		<td><a class="ajax_action" href="{{ path('stock_etat_update', {index : etat.id} ) }}"><i class="icon-pencil"></i></a></td>
		</tr>
		{% endfor %}
		</tbody>
		</table>
	</div>
	
	<div id="ajax_result"></div>

{% endblock %}
