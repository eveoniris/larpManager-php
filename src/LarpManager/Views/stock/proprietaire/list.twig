<ul class="list-group">
	<a href="{{ path('stock_proprietaire_add') }}" class="list-group-item active">
		<span class="badge"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span></span>
		<h4 class="list-group-item-heading">Ajouter un propriètaire</h4>
	</a>
	{%  for proprietaire in proprietaires %}
		<a href="{{ path('stock_proprietaire_update', {index: proprietaire.id}) }}" class="list-group-item">
			<span class="badge">{{ proprietaire.objets|length }}</span>
			<h4 class="list-group-item-heading">{{ proprietaire.nom }}</h4>
			{{ proprietaire.mail }}&nbsp;{{ proprietaire.tel }}
		</a>
	{%  endfor %}
</ul>