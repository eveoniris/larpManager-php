<form action="{{ path('stock_objet_add') }}" method="POST" class="pure-form">
	<fieldset>
		<legend>Ajout d'un objet</legend>
		{% form_theme form 'Form/fields.twig' %}
		{{ form(form) }}
	</fieldset>
</form>