{% extends "layout.twig" %}

{% block content %}

	<ol class="breadcrumb">
	  <li><a href="{{ path('stock_homepage')}}">Stock</a></li>
	  <li><a href="{{ path('stock_etat_index') }}">Tag</a></li>
	  <li class="active"><a href="#">Ajout</a></li>
	</ol>

	<div class="page-header">
		<h1>Ajout d'un tag</h1>
	</div>

	<form action="{{ path('stock_tag_add') }}" method="POST">
			{% form_theme form 'Form/fields.twig' %}
			{{ form(form) }}
	</form>

{% endblock %}