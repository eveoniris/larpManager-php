{% extends "layout.twig" %}

{% block content %}

	<div class="page-header">
		<h1>Ajout d'un utilisateur</h1>
	</div>
	
	<div class="container-fluid">
	<div class="row">
		<div class="col-xs-12 col-md-8">
			<p>Cette page vous permet d'ajouter un utilisateur. Un mot de passe aléatoire lui sera alloué, et un mail automatique sera envoyé pour le prévenir.</p>
			<p>Cette opération permet d'éviter d'attendre qu'un joueur créé lui-même son accés à larpManager.</p>
			
			<form action="{{ path('user.add') }}" method="POST" {{ form_enctype(form) }}>
				{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}
			
				{{ form(form) }}
			</form>
		</div>
	</div>

{% endblock content %}