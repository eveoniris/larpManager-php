{% extends "layout.twig" %}

{% block title %}Billets{% endblock title %}

{% block content %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li><a href="{{ path('billet.list') }}">Liste des billets</a></li>
		<li class="active">Ajout d'un billet</li>
	</ol>
	
	<div class="well bs-component">
		<form action="{{ path('billet.add') }}" method="POST" {{ form_enctype(form) }}  novalidate>
			<fieldset>
				<legend>Ajout d'un billet</legend>
				{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}
	
				{{ form(form) }}
			</fieldset>
		</form>
	</div>
	
{% endblock %}