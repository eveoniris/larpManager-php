{% extends "layout.twig" %}

{% block title %}Groupes{% endblock title %}

{% block content %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li><a href="{{ path("groupe.admin.list") }}">Liste des groupes</a></li>
		<li><a href="{{ path("groupe.detail", {'index': groupe.id}) }}">{{ groupe.nom }}</a></li>
		<li class="active">Ajouter un participant</li>
	</ol>
	
	<div class="well well-sm">
		<form action="{{ path('groupe.admin.participant.add',{'groupe': groupe.id}) }}" method="POST">
			<p>Ce formulaire ne liste que les utilisateurs n'étant pas inscrit dans un groupe</p>
			{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}		
			{{ form(form) }}
		</form>
	</div>
{% endblock %}