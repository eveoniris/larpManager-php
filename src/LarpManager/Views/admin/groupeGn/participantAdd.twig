{% extends "layout.twig" %}

{% block title %}Groupes{% endblock title %}

{% block content %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li><a href="{{ path("groupe.admin.list") }}">Liste des groupes</a></li>
		<li><a href="{{ path("groupe.detail", {'index': groupeGn.groupe.id}) }}">{{ groupeGn.groupe.nom }}</a></li>
		<li><a href="{{ path("groupeGn.list", {'groupe': groupeGn.groupe.id}) }}">Liste des sessions</a></li>
		<li class="active">Ajouter un joueur à une session</li>
	</ol>
	
	<div class="well well-sm">
		<blockquote>Vous allez ajouter un joueur au groupe {{ groupeGn.groupe }} pour la session {{ groupeGn.gn }}.
		Ne sont présenté dans la liste ci-dessous que les joueurs n'étant pas encore affecté à un groupe pour le jeu {{ groupeGn.gn }}</blockquote>
		<form action="{{ path('groupeGn.participants.add',{'groupeGn': groupeGn.id}) }}" method="POST">
			{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}
			{{ form(form) }}
		</form>
	</div>
{% endblock %}