{% extends "layout.twig" %}

{% block content %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li><a href="{{ path('background.list') }}">Liste des background</a></li>
		<li class="active">Ajout d'un background</li>
	</ol>
	
	<div class="well bs-component">
		<form action="{{ path('chronologie.admin.add') }}" method="POST" {{ form_enctype(form) }}  novalidate>
			<fieldset>
				<legend>Ajout d'un événement</legend>
				{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}
	
				{{ form(form) }}
			</fieldset>
		</form>
	</div>
	
	{#
	<form action="{{ path('chrono_add') }}" method="POST">
		<div>
			<label for="paysId">Pays</label>
			<select name="paysId">
			{% for item in pays %}
            	<option value="{{ item.id }}">{{ item.nom }}</option>
        	{% endfor %}
        	</select>
			<label for="date">Date</label>
			<input type="text" name="date" />
			<label for="description">Description</label>
			<input type="text" name="description" />
		</div>
		<input type="submit" value="Enregistrer" />
	</form>
	#}
{% endblock %}