{% extends "layout.twig" %}

{% block title %}Jetons{% endblock title %}

{% block content %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li><a href="{{ path('token.list') }}">Liste des jetons</a></li>
		<li class="active">Ajout d'un jeton</li>
	</ol>
	
	<div class="well bs-component">
		<form action="{{ path('token.add') }}" method="POST" {{ form_enctype(form) }}  novalidate>
			<fieldset>
				<legend>Ajout d'un jeton</legend>
				{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}
	
				{{ form(form) }}
			</fieldset>
		</form>
	</div>
	
{% endblock %}